#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"

// Identificador de la app. Se puede cambiar por uno propio en
// Firma.xcconfig, sin tocar el proyecto de Xcode.
FLUTTER_BUNDLE_ID = com.hexacore.javeriana.appmovil

// Firma de cada desarrollador: no se versiona, y si no existe no pasa
// nada (el simulador no necesita firma). Ver Firma.xcconfig.ejemplo.
#include? "Firma.xcconfig"
