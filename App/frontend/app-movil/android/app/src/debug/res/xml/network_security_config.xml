<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP en claro SOLO contra el backend de desarrollo local.

  Desde Android 9 el tráfico sin cifrar está bloqueado por defecto, y el
  Servicio de Entradas corre en desarrollo sobre http://. En vez de abrir
  `usesCleartextTraffic="true"` para todo, aquí se listan únicamente los
  destinos locales: 10.0.2.2 es el alias con que el emulador ve la máquina
  anfitriona, y localhost cubre el caso de `adb reverse`.

  Este archivo vive en src/debug/, así que NO forma parte de la app publicada:
  en release el tráfico sin cifrar sigue prohibido, como pide RNF-05.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
